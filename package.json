{
  "name": "allright-quiz-qa",
  "version": "1.0.0",
  "private": true,
  "description": "Business-outcome coverage for the All Right registration quiz (Charlie), resilient to A/B variants and step changes.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "@types/node": "^20.14.0",
    "typescript": "^5.5.4"
  }
}
